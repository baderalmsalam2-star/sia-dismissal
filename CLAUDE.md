# نداء الانصراف — أكاديمية سلطان الدولية

موقع ثابت (HTML/CSS/JS بدون أي أدوات بناء) لنداء الطلبة وقت الانصراف.
المستودع `sia-insraf/sia-insraf.github.io`، ويُنشر تلقائيًا على GitHub Pages عند كل push إلى `main`:
https://sia-insraf.github.io/

اسم المستودع نفسه هو الدومين، فالموقع يُنشر على جذر الدومين بلا مجلد فرعي.
كل المسارات في `index.html` نسبية — لا تستخدم مسارات تبدأ بـ `/` حتى يبقى قابلًا للنقل.

## الملفات
- `index.html` — الصفحة الوحيدة. **عند أي تعديل على app.js أو app.css ارفع رقم `?v=` فيها** حتى تحمّل التلفزيونات والجوالات النسخة الجديدة.
- `app.js` — كل المنطق: التوجيه عبر الـ hash (`#k=<رمز المدرسة>&v=home|call|screen|manage&code=<رمز>`، وصفحة ولي الأمر `#p=…`)، المزامنة مع Firebase عبر REST + EventSource (بدون SDK).
- `app.css` — التصميم. ألوان الشعار: كحلي `#3d3160` وتركوازي `#aecfd4`.
- `config.js` — رابط قاعدة البيانات `dbUrl`.
- `firebase-rules.json` — قواعد أمان قاعدة البيانات، تُلصق في Firebase ← Rules.
- `seed.js` — القائمة الأولية للأسماء، **غير موجود في المستودع عمدًا** (`.gitignore`). الأسماء تُحفظ في Firebase فقط.

## البيانات (Firebase Realtime Database تحت `schools/<رمز المدرسة>/`)
- `buildings/{id}`: `{ name, code, desc, addr, map, order }`
- `students/{id}`: `{ n: الاسم, c: رمز الصف مثل "BG1A" أو "GG1B", b: معرّف المبنى }`
- `calls/{studentId}`: `{ t: وقت النداء, o?: وقت الخروج }` — تُهمل النداءات من الأيام السابقة.
- `classes/{رمز الصف}`: `true` — صفٌّ مُعلَن يظهر في قوائم الصفوف ولو ما فيه طالب.
  قوائم الصفوف = صفوف الطلبة + هذه.
- `settings/minutes`: بعد كم دقيقة يعتبر الطالب خارجًا تلقائيًا (0 = أبدًا).
- `settings/pin`: الرقم السري لصفحة التوزيع (فاضي = بلا قفل).
- `settings/inbox`: مفتاح صندوق طلبات أولياء الأمور، و`settings/salt` ملح بصمة الرقم
  المدني. `lat` و`lng` و`rad` موقع المدرسة ونصف القطر بالمتر، و`h1` و`h2` ساعات
  استقبال الطلبات بالدقائق من منتصف الليل.
- `clock`: قيمة واحدة يكتبها كل جهاز عند الإقلاع (`.sv: timestamp`) ليعرف وقت الخادم،
  لأن ساعة التلفزيون قد تكون غلط فتفرغ الشاشة. لا يقرأها أحد.

## أولياء الأمور (خارج عقدة المدرسة)
**الرابط العام واحد للجميع**: `#i=<مفتاح الصندوق>` — بلا رمز مدرسة ولا رمز طالب.
ولي الأمر يضيف ولده بالرقم المدني مرة وحدة، وجهازه يتذكّره.

الرقم المدني **لا يُحفظ ولا يُرسل**: يتحوّل على الجهاز إلى بصمة
`SHA-256("<الملح>:<الرقم>")` أول ٤٠ حرفًا، والمقارنة بالبصمة وحدها. الملح
(`settings/salt`) خاص بالمدرسة حتى لا تنفع جداول جاهزة لكشف الأرقام.

يبقى أيضًا **الرابط الفردي** لمن يبي ضغطة بلا رقم:
`#p=<رمز الطالب>&n=<اسمه>&i=<مفتاح الصندوق>`، واسم الطالب فيه يجي من الرابط لا من
قاعدة البيانات. الطريقان يكتبان في نفس الصندوق، والمدرسة تطابق `p` أو `h`.

- `students/{id}.h`: بصمة الرقم المدني (بطاقة «الأرقام المدنية» في التوزيع).
- `students/{id}.p`: رمز الطالب (٢٤ حرفًا) لزر «أنشئ روابط الجدد».
- `p/<البصمة>`: `{ n: اسم الطالب }` — لا يُقرأ إلا بمعرفة البصمة نفسها (أي بمعرفة
  الرقم المدني)، وبه تعرض صفحة ولي الأمر الاسم للتأكيد. ورمز الرابط الفردي يُسجَّل `true`.
- `inbox/<مفتاح الصندوق>/<الرمز>`: `{ t: وقت الطلب, d?: المسافة بالمتر }` — يكتبه ولي الأمر.
  أي جهاز مدرسة مفتوح يحوّله إلى نداء (`calls/{id}` بـ `r: 1`) ثم يمسحه خلال ثانية.
- `pub/<مفتاح الصندوق>`: `{ s: الملح, lat, lng, r, h1, h2 }` — ما تحتاجه صفحة ولي الأمر وحده.
- `calls/{id}.r = 1` يعني طلب ولي أمر لا نداء المواقف، ويظهر بوسم 👪.

صفحة الشاشة (`viewScreen`) **للعرض فقط**: لا ضغط على اسم ولا تسجيل خروج — النداء
والخروج في صفحة النداء وحدها، حتى لا تُخرج لمسةٌ عابرة على تلفزيون أو جوال معلمة طالبًا.

صفحة ولي الأمر (`viewParent`) تحفظ أولاده في `localStorage` (بصمة واسم، بلا رقم مدني)،
والرابط في شريط العنوان يُعاد بناؤه ليحملهم كلهم حتى تنفع إضافته للشاشة الرئيسية.

قواعد الأمان في `firebase-rules.json` — أي مسار جديد لازم يُضاف فيها وإلا رُفضت الكتابة.

رمز المدرسة سري ولا يُكتب في المستودع (يُمرَّر في الرابط فقط).

## المباني
| الاسم | الرمز | المرحلة |
|---|---|---|
| مبنى 25 | 25 | ابتدائي بنين G1–G5 |
| مبنى 10 | 10 | متوسط بنين G6–G8 |
| مبنى 6 | 6 | ثانوي بنين G9–G12 |
| مبنى 15 | 15 | البنات، كل المراحل |

## رموز الصفوف — الصيغة المعتمدة
`BG1A` = بنين، Grade، المرحلة 1، الشعبة A. و`GG1B` = بنات، المرحلة 1، الشعبة B.

| الجزء | المعنى |
|---|---|
| الحرف الأول `B` / `G` | الفئة: Boys / Girls |
| `G` | Grade |
| الرقم | المرحلة (1–12) |
| الحرف الأخير | الشعبة: A أو B — جزء من الرمز دائمًا |

هكذا تُكتب قيمة `c` في `students/{id}`، وهكذا تُولَّد روابط الصفوف.
صفٌّ بشعبة واحدة يُكتب `A` (مثل `BG1A`)، وأداة التحويل في صفحة التوزيع تفترض `A` لأي رمز قديم بلا حرف.

وكرمز **دخول** فقط، `BG1` بلا حرف تفتح المرحلة كاملة بكل شعبها.

## رموز الدخول (`resolveCode` في app.js)
رقم فقط ← المبنى بهذا الرمز. `BG1A` / `GG1B` ← شعبة، و`BG1` ← المرحلة كاملة.
`بنات 1` ← صف البنات. غير ذلك ← بحث في أسماء الصفوف (مثل "رعاية" أو اسم معلمة).

الصيغ القديمة (`G5` و`G5A` و«بنات G6») ما زالت تعمل للبيانات غير المحدَّثة، لكن
`B6` لم تعد تعني البنات: الحرف `B` صار يعني البنين في الصيغة المعتمدة.

طلبة الرعاية في مبنى 25 مسجّلون بألقابهم فقط: الدعيج، اليماني، المطيري، العسعوسي. أبرار وميساء وإلهام **معلمات** لا طالبات — أسماؤهن تُستخدم كاسم الصف حتى تفتح المعلمة صفحتها بكتابة اسمها.

## التجربة
افتح `index.html` عبر أي خادم ثابت. بدون رمز مدرسة في الرابط يعمل "وضع تجريبي" على الجهاز نفسه.
