# نداء الانصراف — أكاديمية سلطان الدولية

موقع ثابت (HTML/CSS/JS بدون أي أدوات بناء) لنداء الطلبة وقت الانصراف.
يُنشر تلقائيًا على GitHub Pages عند كل push إلى `main`:
https://baderalmsalam2-star.github.io/sia-dismissal/

## الملفات
- `index.html` — الصفحة الوحيدة. **عند أي تعديل على app.js أو app.css ارفع رقم `?v=` فيها** حتى تحمّل التلفزيونات والجوالات النسخة الجديدة.
- `app.js` — كل المنطق: التوجيه عبر الـ hash (`#k=<رمز المدرسة>&v=home|call|screen|manage&code=<رمز>`)، المزامنة مع Firebase عبر REST + EventSource (بدون SDK).
- `app.css` — التصميم. ألوان الشعار: كحلي `#3d3160` وتركوازي `#aecfd4`.
- `config.js` — رابط قاعدة البيانات `dbUrl`.
- `seed.js` — القائمة الأولية للأسماء، **غير موجود في المستودع عمدًا** (`.gitignore`). الأسماء تُحفظ في Firebase فقط.

## البيانات (Firebase Realtime Database تحت `schools/<رمز المدرسة>/`)
- `buildings/{id}`: `{ name, code, desc, addr, map, order }`
- `students/{id}`: `{ n: الاسم, c: الصف مثل "G5A" أو "بنات G6", b: معرّف المبنى }`
- `calls/{studentId}`: `{ t: وقت النداء, o?: وقت الخروج }` — تُهمل النداءات من الأيام السابقة.
- `settings/minutes`: بعد كم دقيقة يعتبر الطالب خارجًا تلقائيًا (0 = أبدًا).

رمز المدرسة سري ولا يُكتب في المستودع (يُمرَّر في الرابط فقط).

## المباني
| الاسم | الرمز | المرحلة |
|---|---|---|
| مبنى 25 | 25 | ابتدائي بنين G1–G5 |
| مبنى 10 | 10 | متوسط بنين G6–G8 |
| مبنى 6 | 6 | ثانوي بنين G9–G12 |
| مبنى 15 | 15 | البنات، كل المراحل |

## رموز الدخول (`resolveCode` في app.js)
رقم فقط ← المبنى بهذا الرمز. `G5` / `grade five` / `صف خامس` ← صف البنين. `G5A` / `5A` ← شعبة. `B6` / `بنات 6` ← صف البنات. غير ذلك ← بحث في أسماء الصفوف (مثل "رعاية" أو اسم معلمة).

طلبة الرعاية في مبنى 25 مسجّلون بألقابهم فقط: الدعيج، اليماني، المطيري، العسعوسي. أبرار وميساء وإلهام **معلمات** لا طالبات — أسماؤهن تُستخدم كاسم الصف حتى تفتح المعلمة صفحتها بكتابة اسمها.

## التجربة
افتح `index.html` عبر أي خادم ثابت. بدون رمز مدرسة في الرابط يعمل "وضع تجريبي" على الجهاز نفسه.
